<tr>
    <td><%= link_to(simple_row.team_game.game.name, game_path(simple_row.team_game.game)) %></td>
    <td><%= simple_row.points %></td>
    <% GameStat::TYPE.each do |index| %>
        <td>
          <p><%= simple_row[index] %></p>
        </td>
    <% end %>
</tr>